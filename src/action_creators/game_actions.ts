import { Dispatch } from "redux";
import { Game_Action } from "../action_intefaces/game_interface";
import { Action_Type } from "../action_types";

export const generate_new_game =
  () => async (dispatch: Dispatch<Game_Action>) => {
    try {
    } catch (error) {}
  };
